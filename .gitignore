# 의존성 모듈 (절대 올리면 안 됨)
node_modules/

# 환경 변수 (API 키 등 비밀번호가 들어있음 - 보안 필수)
.env
.env.local
.env.*.local

# 빌드 결과물
dist/
build/

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Mac 시스템 파일
.DS_Store

# 설정 파일 폴더 무시
src/components/config/